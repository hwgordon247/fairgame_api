# fairgame_api

##Setup Instructions

```
npm run setup

npm run start_db

npm start

npm test

```


#### Requirements
node, npm, mongodb

#### Mongodb setup

```
brew install mongodb

sudo mkdir /data

sudo chown -R `id -u` /data
```
